<h1>J'édite ma fiche</h1>

  <%= simple_form_for [@speciality, @chapter, @fiche], url: speciality_chapter_fich_path do |f| %>
  <%= f.error_notification %>
  <%= f.input :title, label: "Titre de la fiche"%>
  <%= f.input :content, as: :file %>

  <%= f.button :submit %>
  <% end %>
