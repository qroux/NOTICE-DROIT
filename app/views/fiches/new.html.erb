<h1>Je crée ma fiche</h1>

  <%= simple_form_for [@speciality, @chapter, @fiche] do |f| %>
  <%= f.error_notification %>
  <%= f.input :title, label: "Titre de la fiche"%>
  <%= f.input :content, as: :file %>

  <%= f.button :submit %>
  <% end %>
